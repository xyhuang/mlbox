schema_type: mlcommons_box_platform
schema_version: 0.1.0

platform:
  name: "docker"
  version_filter:
    good_list: ["19.03"]
runner_configs:
- action: "run"
  task: "download"
  exec:
    type: "container"
    container:
      image: "mlperf/mlbox:mnist"
- action: "run"
  task: "train"
  exec:
    type: "container"
    container:
      image: "mlperf/mlbox:mnist"
